# mind-os
MindOS is a clean, offline first personal workspace inspired by Notion, built as a single HTML file. It includes a dashboard, daily planner, second brain, decision matrix, and journal, all running fully in the browser using localStorage with no backend, no accounts, and full privacy.
